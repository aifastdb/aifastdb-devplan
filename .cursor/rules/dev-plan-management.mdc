---
description: aifastdb-devplan 项目的 DevPlan 配置。包含项目标识、文档层级、开发阶段。通用的 DevPlan 工作流规则由用户级全局规则提供。
globs: []
alwaysApply: true
mcpServers: aifastdb-devplan
---

# aifastdb-devplan 项目 — DevPlan 配置

> **MCP Server**: `aifastdb-devplan`  
> **项目定位**: 基于 aifastdb 存储引擎的通用开发计划管理 MCP Server

## 项目标识

```
projectName = "aifastdb-devplan"
```

## 文档层级

| 层级 | 文件 | 用途 | 大小 |
|------|------|------|------|
| Layer 1 (鸟瞰) | [aifastdb-devplan-overview.md](mdc:docs/aifastdb-devplan-overview.md) | AI 首选阅读，< 500 行，一次读完 | ~320 行 |
| Layer 2 (详细) | [DEVPLAN_ReadMe.md](mdc:DEVPLAN_ReadMe.md) | 完整功能文档，分段读取 | 2100+ 行 |
| Layer 3 (实时) | `.devplan/aifastdb-devplan/` | MCP 工具查询任务状态 | — |

## 快捷操作

### 查看项目进度
```
devplan_get_progress(projectName: "aifastdb-devplan")
```

### 查看任务列表
```
devplan_list_tasks(projectName: "aifastdb-devplan")
devplan_list_tasks(projectName: "aifastdb-devplan", parentTaskId: "phase-1")
devplan_list_tasks(projectName: "aifastdb-devplan", status: "pending")
```

### 完成任务
```
devplan_complete_task(projectName: "aifastdb-devplan", taskId: "T1.1")
```

### Git 同步检查
```
devplan_sync_git(projectName: "aifastdb-devplan", dryRun: true)
devplan_sync_git(projectName: "aifastdb-devplan")
```

### 查看/更新文档片段
```
devplan_list_sections(projectName: "aifastdb-devplan")
devplan_get_section(projectName: "aifastdb-devplan", section: "overview")
devplan_get_section(projectName: "aifastdb-devplan", section: "technical_notes", subSection: "multi-project-routing")
```

### 导出 Markdown
```
devplan_export_markdown(projectName: "aifastdb-devplan", scope: "tasks")
devplan_export_markdown(projectName: "aifastdb-devplan", scope: "full")
```

### 功能模块管理
```
devplan_list_modules(projectName: "aifastdb-devplan")
devplan_get_module(projectName: "aifastdb-devplan", moduleId: "<模块ID>")
```

### Prompt 日志
```
devplan_save_prompt(projectName: "aifastdb-devplan", content: "用户 Prompt", relatedTaskId: "phase-X")
devplan_list_prompts(projectName: "aifastdb-devplan")
devplan_list_prompts(projectName: "aifastdb-devplan", date: "2026-02-18")
devplan_list_prompts(projectName: "aifastdb-devplan", relatedTaskId: "phase-1")
```

## 开发阶段概览

| 阶段 | 状态 | 说明 |
|------|------|------|
| 阶段一 | ✅ 已完成 | 多项目路由完善与测试 (5/5) |
| 阶段二 | ⬜ 待开发 | Autopilot HTTP API 开发 |
| 阶段三 | ⬜ 待开发 | Autopilot MCP 工具 |
| 阶段四 | ⬜ 待开发 | Python Executor 核心 |
| 阶段五 | ⬜ 待开发 P1 | Executor GUI 自动化与视觉分析 |
| 阶段六 | ⬜ 待开发 P1 | Executor Web UI 与集成测试 |

## 注意事项

1. **优先读概览文件**：初次了解项目时，先读 [aifastdb-devplan-overview.md](mdc:docs/aifastdb-devplan-overview.md)（< 500 行），再按需深入 [DEVPLAN_ReadMe.md](mdc:DEVPLAN_ReadMe.md) 的具体章节
2. **数据存储位置**：本项目的 devplan 数据存储在 `D:\Project\git\aifastdb-devplan\.devplan\aifastdb-devplan\`（非 ai_db 目录下）
3. **源码即产品**：本项目的 `src/` 目录就是 DevPlan MCP Server 的源码，修改后需 `npm run build` 并重启 Cursor
